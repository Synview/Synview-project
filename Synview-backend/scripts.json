{
  "$schema": "https://deno.land/x/denon@2.5.0/schema.json",

  "scripts": {
    "start": {
    "cmd": "denon run --allow-env --allow-read --allow-ffi --allow-net --env=.env index.ts",
      "desc": "run my app.ts file",
      "env": {
        "file": ".env"
      }
    }
  }
}
